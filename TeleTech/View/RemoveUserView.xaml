<UserControl x:Class="TeleTech.View.RemoveUserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TeleTech.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <ComboBox x:Name="AccountStatusComboBox" ItemsSource="{Binding AccountStatusList}"
                      DisplayMemberPath="AccountStatusName" SelectedValuePath="Id"
                      SelectedItem="{Binding SelectedAccountStatus,Mode=TwoWay}"/>
            <Button Content="Сохранить" Command="{Binding SaveAccountStatusChangesCommand}" CommandParameter="{Binding SelectedAccountStatus}"/>
        </StackPanel>
        
    </Grid>
</UserControl>
